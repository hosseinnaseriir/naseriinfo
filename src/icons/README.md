# Icons

Store project SVG icons in this directory.

- Keep SVGs optimized.
- Use descriptive names like `calendar.svg`, `arrow-right.svg`.
- Prefer `currentColor` for strokes/fills to support theming.
