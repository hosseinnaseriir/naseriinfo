---
interface Props {
  class?: string;
  gap?: "sm" | "md" | "lg";
}

const { class: className = "", gap = "md" } = Astro.props;
const gapMap = { sm: "gap-2", md: "gap-4", lg: "gap-6" };
---

<div class={`flex flex-col ${gapMap[gap]} ${className}`}>
  <slot />
</div>
