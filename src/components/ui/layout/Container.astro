---
interface Props {
  as?: "div" | "section" | "main" | "header" | "footer";
  class?: string;
}

const { as = "div", class: className = "" } = Astro.props as Props;
const Tag = as;
---

<Tag class={`mx-auto w-full max-w-(--container-max) px-3 sm:px-4 ${className}`}>
  <slot />
</Tag>
