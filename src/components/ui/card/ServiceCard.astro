---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<article class="surface p-4">
  <h3 class="text-xl font-semibold">{title}</h3>
  <p class="mt-2 text-base text-[var(--color-text-muted)]">{description}</p>
</article>
